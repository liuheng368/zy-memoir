# `_shared` — Skills 公共代码

本目录放**多个 Skill 共用**的脚本与模板，避免在单个 skill 目录内重复维护。

| 子目录 | 用途 |
|--------|------|
| **`create-feature-spec/`** | Feature 骨架：`spec`（需求）/ `plan`（技术 + UI/Token）；脚本 + `templates/`（**spec-creator** 调用） |
| **`redhi-*.mjs`** | RedHi / Git 相关（如 **review-spec-push** 使用） |
