# {{FEATURE_TITLE}} — 技术方案

> **文档分工**：**`plan.md`** 承载**技术方案**（文档标题）：**`## 概述`**、**`## 现状分析`**、**`## 页面交互细节`**（布局引用 spec/source、按模块拆交互、**本页**接口 path）、**`## API 梳理`**（本次设计涉及接口：**方法**、**路径**、**Response**、**字段映射**）、**`## 架构设计`**（含 **功能模块划分**与 **`### 技术选型 / 约束`**；**`kmp/`、`android/`** 模块职责与路径写于此节或 **`## 实现计划`**）、**`## 技术方案`**（实现级：**文件改动清单**、**新增字段（预估）**）、**`## 方案澄清事项`**（与 [`spec.md`](./spec.md) **`### 澄清事项`** 形态一致：**`#### Qn`** + **`- [ ]`…`⚠️` / `- [x]`…`✅`**；**方案 / 实现向**，供研发或评审选定）、**`## 风险与未决事项`**、**`## 验收标准`（AC）**、**`## 开发任务`**（层级勾选清单：按文件/Screen 拆解 UI 与逻辑）、**`## 实现计划`**、**`## 代码审查`**。全链路数据流、跨页契约、Design Token 等仍写入 **`## 架构设计` / `### 技术选型 / 约束`** 或 **`## 实现计划`**。**行为级页面描述与线框/截图** 以 [`spec.md`](./spec.md) **`## 页面交互描述`**（及 [`source/`](./source/)）为准；**`plan.md`** 本节做**工程向细化**并与 spec **互链**（布局大图不在 plan 独占时，须写明 spec 章节锚点与 `source/` 相对路径）。**原始 PRD / 产品文档**留存于 [`prd/`](./prd/)（与 **`source/`** 分工见各自 **README**）。与 **`.cursor/skills/feature/workflow-reference.md`** 一致。
>
> **创建日期**：{{DATE_ISO}}

## 概述

<!-- 本需求在系统中的位置、与上下游关系、非功能约束；与 spec 中需求对齐 -->

-

---

## 现状分析

<!-- 仓库内已有能力盘点，避免重复造轮子；路径与条目须与「页面交互细节」「API 梳理」「架构设计」「技术方案」「方案澄清事项」「开发任务」「实现计划」互链或承接 -->

### 已有基础设施

-

### 已有可复用 i18n 字符串

-

### 已有可复用的 UI 组件

-

---

## 页面交互细节

<!-- 以 spec「## 页面交互描述」/ §1 页面结构为输入，按页或按主 Screen 填写；与「API 梳理」「方案澄清事项」「验收标准」「开发任务」「实现计划」编号可互链 -->

### 页面布局与素材引用（对齐 spec）

<!-- 列出本页对应的 spec 小节标题或锚点；线框/截图放在 spec 嵌入或 ./source/（写相对路径，如 source/pages/xxx.png）。模块区块编号须与 spec 图中 ①②③… 或 ASCII 分块一致 -->

-

### 模块交互拆解

<!-- 按模块逐条：组件复用、文案/i18n key、可见性条件、手势与导航、Bottom Sheet / 系统返回、错误与空态、与 AC 编号映射 -->

-

### 页面涉及接口

<!-- 本页范围内 HTTP 方法 + path、触发时机（进入页 / 切换开关 / 提交）；与「模块交互拆解」条目对应。**契约正文**（Response 示例、字段含义、枚举）写在 **`## API 梳理`** 对应条目，此处可互链 path 避免重复 -->

-

---

## API 梳理

<!-- 列出本次需求涉及的**全部**接口（可多 `### API-n`）；每条须含四块：**方法**、**路径**、**Response Payload**（JSON 示例或 Schema 摘要）、**字段映射**表。编号建议 `4.1` / `4.2` 与场景标题（如「查询设置 — 进入页面时调用」）或统一 `### API-1`；嵌套字段用 `obj[].key` 点分路径。与「页面交互细节」模块编号 ①②③…、**`## 验收标准`** 可交叉引用。无网络接口则整节 **N/A** -->

### API-1：（场景 — 调用时机）

- **方法**：`GET`
- **路径**：`/api/...`

**Response Payload：**

```json
{}
```

**字段映射：**

| API 字段 | 类型 | 对应 UI | 说明 |
| -------- | ---- | -------- | ---- |
|          |      |          |      |

---

## 架构设计

<!-- 系统架构概览、功能模块划分；接口/数据流/UI/Token 等并入本节或「技术选型」、实现计划条目。须与 android/ARCH.md、DEV_GUIDE 不矛盾 -->

### 技术选型 / 约束

-

---

## 技术方案

<!-- 实现级落地（与文档标题「— 技术方案」区分：本节为**文件级**与**状态字段**预估）；须与 **`## 架构设计`**、**`## 开发任务`**、**`## 实现计划`** checkbox 一致，路径为仓库相对路径 -->

### 文件改动清单

| 文件 | 改动类型 | 状态 | 说明 |
| ---- | -------- | ---- | ---- |
|      | 修改 / 新增 | ⬜ |      |

### 新增字段（预估）

<!-- 主要为 **UiState** / **ViewModel** / **Screen 级 State** 等；用分组注释（如 `// 主态` `// 客态`）；语言与工程一致（默认 **Kotlin**） -->

```kotlin
// 示例：val foo: Boolean = true

```

---

## 方案澄清事项

<!-- **与 `spec.md` `### 澄清事项` 分工**：**spec** 收**需求 / 产品**待确认；**plan** 本节收**实现方案分叉**（动效路径、导航栈、状态拆分、技术选型 A/B 等），供**研发或评审勾选**。**形态须一致** spec-creator「澄清事项」：**`#### Qn：主题`** 下 **`- [ ]`…`⚠️`**（待选）或 **`- [x]`…`✅ 已确认`**；可互链 **`spec.md`** **`## 页面交互描述`**、**`## 开发任务`**。无方案分叉则写 **无** 或 **N/A** -->

#### Q1：<一句话主题（动效 / 路由 / 拆分等）>

- [ ] <方案选项或路径描述> ⚠️
- [ ] <另一方案或约束> ⚠️

<!-- 已定案示例（与 spec 相同写法）：
- [x] 动效路径：**先短暂停留往期展示页**，再自动跳转本期消费页（**不**采用「直接进消费页、往期页不可见」）✅ 已确认
- [x] 从消费页返回时「**整屏收起变回周刊卡片**」与进入动效（首位放大进入消费）**互为逆向** ✅ 已确认
-->

---

## 风险与未决事项

-

---

## 验收标准

### AC-1：

- **输入**：
- **操作**：
- **预期**：

---

## 开发任务

<!-- 层级 **`- [ ]` / `- [x]`** 清单：顶层按 **`kmp/`、`android/`** 文件或 **Screen** 聚类（与「技术方案·文件改动清单」对应）；子项拆 **UI 区块**（顶栏、Hint、开关、Bottom Sheet、列表…）、逻辑、联调；依赖设计/运营未闭环的条目标 **`⚠️`**；与「页面交互细节」模块编号、**`## 实现计划`** 组内 checkbox 互链。无增量则本节 **N/A** -->

- [ ] **`path/to/ExampleScreen.kt`** — 简述（路由常量、`when` 分支、入口等）
  - [ ] 子任务：例如复用 `SettingTopBar`
  - [ ] 子任务：主态 / 客态 UI 与状态绑定
  - [ ] **⚠️** 占位图标替换为正式资源（人工 / 设计）
- [ ] **API / 联调**（可选）
  - [ ] 契约与 **`## API 梳理`** 一致
  - [ ] 联调通过

---

## 实现计划

<!-- 阶段 2 填写：按组拆分 checkbox，格式见 .cursor/skills/feature/workflow-reference.md -->

---

## 代码审查

<!-- 阶段 4 填写：实现计划全部 [x] 后 -->

- **结论**：待审查
- **严重问题**：无
- **备注**：
