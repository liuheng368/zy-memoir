# {{FEATURE_TITLE}}

> **Feature 目录**：`docs/features/{{FEATURE_SLUG}}/`
> **创建日期**：{{DATE_ISO}}
>
> **文档分工**：**`spec.md`** 做**需求的详细阐述**：背景与目标、**`## 页面交互描述`**（下述**五**节；**纯交互与串联**，含**多页链路、动效与跳转**；**不写接口**；**不写**工程「涉及模块 / 改动文件」清单，见 [`plan.md`](./plan.md) **「架构设计」功能模块划分**或 **`## 实现计划`**）、**`### 澄清事项`**（**需求 / 产品**向 **`#### Qn`** + 勾选）、分支名。**`## 验收标准`（AC）** 默认写在 [`plan.md`](./plan.md)。**原始 PRD** 放 [`prd/`](./prd/)，**线框截图**等放 [`source/`](./source/)（见各目录 **README**）。**实现方案分叉**（动效路径、导航栈、技术选型 A/B 等）写在 [`plan.md`](./plan.md) **`## 方案澄清事项`**（形态与 **`### 澄清事项`** 一致）。组件复用、Design Token、接口、**文件改动**、**UiState 字段预估**与 **开发任务勾选拆解**写在 [`plan.md`](./plan.md)（**`## 技术方案`**、**`## 开发任务`**、**`## 实现计划`**、**`## 代码审查`** 等）。

## 需求描述

{{DESCRIPTION}}

### 背景与目标

<!-- 补充业务背景、用户故事、非目标（Out of scope） -->

-

## 页面交互描述

> **范围**：**可见结构**、**手势/点击**、**状态变化**；**单页与弹层**及**多页之间**的串联；**动效与转场、页面跳转**的**用户可感知结果**（行为级描述即可）。**不包含**接口 Path、参数、错误码、埋点 key、数据模型字段——此类写在 [`plan.md`](./plan.md)。

### 1. 页面结构描述

<!-- 顶栏、主内容分区、列表/卡片布局、关键区块命名（可对照线框或设计稿）；单页内信息架构。纵向分区 ≥3 或存在条件展示时，建议加「示意结构图」：一句「垂直布局如下」+ 代码块内 ASCII 框线 + ①②③ 与表格分区一致；图放 source/ 则链相对路径。详见 `.cursor/skills/spec-creator/SKILL.md`「§1 示意结构图」。 -->

-

### 2. 具体区域交互

<!-- 各区块可点范围、开关/列表项、滑动与焦点、空态/禁用态等 -->

-

### 3. 操作点击后效果

<!-- 点击/切换后的 UI 反馈：展开收起、Toast、字段联动、乐观 UI 与失败回退语义；全屏跳转 / Bottom Sheet / 同页内展开；转场或动画的**用户感知**（如「先过渡页再进入消费页」「整屏收起回到列表卡片」）；仍不写 API 与具体 duration/曲线参数（除非产品已规定并需进 AC） -->

-

### 4. 页面串联关系

<!-- 本页与**直接相邻**页面或弹层：进入/返回、系统返回键、条件显示（如某模式下才展示子列表）；deeplink 再入时落在哪一屏 -->

-

### 5. 多页面串联

<!-- **跨多屏主路径与分支**（例：首页 → 子页 → 返回恢复位置）；**用户可见**的页面名 / 流程名 / 入口（可与澄清 Qn、设计稿互链）；与任务栈、deeplink、系统返回相关的**用户可见**结果；可用分步列表或 Mermaid flowchart（不写 API）。**Gradle 模块、kmp/android 路径、改动矩阵**写在 [`plan.md`](./plan.md)，**勿**在本节列「涉及模块」表。 -->

-

### 澄清事项

<!-- 默认形态见 `.cursor/skills/spec-creator/SKILL.md`「澄清事项」：每条用 #### Qn + 任务列表，勿仅有表格。 -->

#### Q1：<一句话主题>

- [ ] <待确认描述> ⚠️

<!-- 已定案示例：- [x] <落定描述> ✅ 已确认 -->

---

## 分支名称

`feat/{{FEATURE_SLUG}}`

---

## 关联文档

- **验收标准（AC）**、**技术方案**（**概述**、**现状分析**、**页面交互细节**、**API 梳理**（方法、路径、Response、字段映射）、**架构设计**/**技术选型**（功能模块划分）、**`## 技术方案`**（文件改动清单、新增字段预估）、**`## 方案澄清事项`**（与 **`### 澄清事项`** 同形态；**方案向**）、风险、**`## 开发任务`**、**实现计划**；契约见 **API 梳理**，本页触发与模块拆解见 **页面交互细节**，文件与状态字段见 **`## 技术方案`**，方案勾选见 **`## 方案澄清事项`**，执行拆解见 **`## 开发任务`**，全链路并入架构或实现计划）：[`plan.md`](./plan.md)
- **原始 PRD / 产品文档**（PDF、DOCX、PPT 等留存）：[`prd/`](./prd/)（与 [`source/`](./source/) 设计素材分工见该目录 **README**）
- 仓库架构总览：`docs/architecture.md`（若适用）
