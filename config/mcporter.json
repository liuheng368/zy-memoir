{
  "mcpServers": {
    "cloudbase": {
      "command": "npx",
      "args": ["@cloudbase/cloudbase-mcp@latest"],
      "description": "Tencent CloudBase MCP — 通过 device-code 登录后管理云开发后端资源（环境 / 函数 / 数据库 / 存储）",
      "lifecycle": "keep-alive"
    }
  }
}
